# Backlog

- Resolve or explicitly accept the remaining full UI dev-tool audit findings
  for Vite/esbuild when a Vite major upgrade is appropriate.
- Keep schema governance and source-assumption docs current as licensed or
  certified message material becomes available; starter schemas must remain
  clearly labeled as non-certified.
- Add new ADRs only when future work changes an architectural invariant listed
  in [`.context/invariants.md`](../../.context/invariants.md).
- Continue tightening docs around experimental status whenever user-facing
  claims or deployment guidance expand.
- Keep [current work](current.md) pointed at the live
  [`cgpt-queue/`](../../cgpt-queue/) while that queue exists.

Note: Earlier backlog items about adding ADRs, schema/source assumptions, and
experimental-status capture are partially superseded by the ADR set, workflow
docs, and refreshed context files now indexed from [`docs/index.md`](../index.md).
