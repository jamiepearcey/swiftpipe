# Current Work

- Active autonomous hardening work is tracked in
  [`cgpt-queue/`](../../cgpt-queue/). Each `NNN-*.md` file is a
  self-contained task; execute the lowest-numbered unblocked item and delete
  the file only after the task is validated.
- The remaining queue currently focuses on project-memory and documentation
  cleanup after the UI hardening series.

Note: The earlier current task, "Establish durable project memory above the
implementation repo," is superseded by the existing `.context/`, `docs/`, ADR,
workflow, and queue structure.
